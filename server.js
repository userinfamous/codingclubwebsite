var compression=require('compression');var connect=require('connect');var serveStatic=require('serve-static');console.log('\n\n--- Node Version: '+process.version+' ---');var app=connect();app.use(compression({filter:shouldCompress}));app.use(serveStatic("."));app.listen(80);function shouldCompress(req,res){if(req.headers['x-no-compression']){return false}return compression.filter(req,res)}